const fs = require('fs')
const logoGenerator =  require('../lib/logoGenerator')
const { describe } = require('yargs')
class validate{}


describe('',()=> {
    it('Checks if text is 3 characters',()=>{
        const txtSize = new validate();
        const result = 
        if(txtSize.length === 3) {
            
            
        }

    })
})